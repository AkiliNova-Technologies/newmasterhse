import { insight, news } from "./article";
import { podcast, service, siteSettings, teamMember } from "./content";

export const schemaTypes = [news, insight, service, podcast, teamMember, siteSettings];
